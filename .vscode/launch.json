{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Operator TLS (files) but insecure",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceFolder}/cmd/main.go",
      "cwd": "${workspaceFolder}",
      "debugAdapter": "dlv-dap",
      "env": {
        "ENV": "" // "" for base for .env, "test" for .env-test, "dev" for .env-dev, "prod" for .env-prod
      }
    }
  ]
}
