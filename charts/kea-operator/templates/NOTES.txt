KEA Operator has been installed.

PREREQUISITES:
  Before using this operator, ensure you have installed the VitiStack CRDs:
  
    helm install vitistack-crds oci://ghcr.io/vitistack/helm/crds

CONFIGURATION:
  Make sure to configure the KEA server URL:
  
    --set kea.url="https://your-kea-server:8000"

VERIFY:
  Check the operator is running:
  
    kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "kea-operator.name" . }}

